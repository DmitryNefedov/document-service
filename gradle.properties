# Run Gradle itself on JDK 21 (Gradle 9 requires 17+; the build also targets 21
# via the Java toolchain in build.gradle). Adjust to your local JDK 21 path.
org.gradle.java.home=/usr/lib/jvm/java-21-openjdk-amd64
